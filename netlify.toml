cat > /mnt/user-data/outputs/netlify.toml << 'EOF'
[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — must come LAST
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
EOF
echo "done"